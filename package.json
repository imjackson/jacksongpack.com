{
  "scripts": {
    "prepare": "husky install",
    "pre-commit": "pretty-quick --staged",
    "build": "./build",
    "clean": "rm -rf dist/",
    "lint:check": "prettier -c .",
    "lint:write": "prettier -w ."
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "prettier": "^2.7.1",
    "pretty-quick": "^3.1.3"
  }
}
